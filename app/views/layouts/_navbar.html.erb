<style type='text/css'>
  /* nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 8rem;
  } */
  /* #brand {
    font-size: 1.5rem;
    line-height: 2.5rem;
    font-weight: 600;
    color: inherit;
    text-decoration: none;
  } */
  /* #sign_in_out {
    padding-top: 0.5rem;
    padding-bottom: 0.5rem;
    padding-left: 1rem;
    padding-right: 1rem;

    border: solid black 1px;
    border-radius: 0.25rem;
    color: inherit;
    text-decoration: none;
  }
  #sign_in_out:hover {
    background-color: black;
    color: white;
  } */
  /* .links {
    margin-right: 0.5rem;
    color: black;
    text-decoration: none;
  }
  .links:hover {
    text-decoration: underline;
  } */
</style>

<nav>
  <%= link_to 'SCHEDULER', root_path, class: 'brand lg'  %>
  <% if session[:username] %>
    <div>
      <%= link_to 'User', user_path(session[:user_id]), class: 'links' %>
      <%= link_to 'Projects', projects_path, class: 'links' %>
      <%= link_to 'Sign Out', sign_out_path, id: 'sign_in_out' %>
    </div>

  <% else  %>
    <%= link_to 'Sign In', '/sign_in', id: 'sign_in_out' %>
  <% end %>
  <%# <%= link_to '[NAME OF LINK]', [ROUTE]_path, id: '[CSS SELECTOR]' %>
</nav>
